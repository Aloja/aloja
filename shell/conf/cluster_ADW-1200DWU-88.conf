#load AZURE defaults
source "$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )/azure-saas_defaults.conf"

clusterID="$(get_id_cluster "$(basename $BASH_SOURCE)")"
clusterName="ADW-1200DWU-${clusterID}"
numberOfNodes="12" #1200DWU #starts at 0 (max 99)
#Node config
vmSize='1200DWU'

#details
vmCores="6"
vmRAM="42" #in GB

#costs
clusterCostHour="8.40"
clusterType="SaaS"
clusterDescription="Azure DW 1200DWU"

extraLocalCommands=""
