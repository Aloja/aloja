#load AZURE defaults
source "$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )/azure-saas_defaults.conf"

clusterID="$(get_id_cluster "$(basename $BASH_SOURCE)")"
clusterName="ADW-500DWU-${clusterID}"
numberOfNodes="5" #500DWU #starts at 0 (max 99)
#Node config
vmSize='500DWU'

#details
vmCores="6"
vmRAM="42" #in GB

#costs
clusterCostHour="2.95"
clusterType="SaaS"
clusterDescription="Azure DW 500DWU"

extraLocalCommands=""
