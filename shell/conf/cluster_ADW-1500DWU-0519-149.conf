#load AZURE defaults
source "$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )/azure-saas_defaults.conf"

clusterID="$(get_id_cluster "$(basename $BASH_SOURCE)")"
clusterName="ADW-1500DWU-0519-${clusterID}"
numberOfNodes="15" #1500DWU #starts at 0 (max 99)
#Node config
vmSize='1500DWU-0519'

#details
vmCores="6"
vmRAM="42" #in GB

#costs
clusterCostHour="10.50"
clusterType="SaaS"
clusterDescription="Azure DW 1500DWU"

extraLocalCommands=""
