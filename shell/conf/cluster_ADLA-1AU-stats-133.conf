#load AZURE defaults
source "$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )/azure-saas_defaults.conf"

clusterID="$(get_id_cluster "$(basename $BASH_SOURCE)")"
clusterName="ADLA-1AU-stats-${clusterID}"
numberOfNodes="1" #starts at 0 (max 99)
#Node config
vmSize='1AU-stats' #Analytic units

#details
vmCores="1"
vmRAM="6" #in GB

#costs
clusterCostHour="2.09"  #0.050/job + 0.034/min/parallelism =
clusterType="SaaS"
clusterDescription="Azure Data Lake Analytics 1 Paralellism w/ statistics"

extraLocalCommands=""
