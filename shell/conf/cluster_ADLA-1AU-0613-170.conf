#load AZURE defaults
source "$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )/azure-saas_defaults.conf"
defaultProvider="azure_ADLA"
clusterID="$(get_id_cluster "$(basename $BASH_SOURCE)")"
clusterName="ADLA-1AU-0613-${clusterID}"
numberOfNodes="1" #starts at 0 (max 99)
#Node config
vmSize='1AU-0613' #Analytic units

#details
vmCores="1"
vmRAM="6" #in GB

#costs
clusterCostHour="1.045"  #0.050/job + 0.034/min/parallelism =
clusterType="SaaS"
clusterDescription="Azure Data Lake Analytics 1 Paralellism w/ statistics"

extraLocalCommands=""
