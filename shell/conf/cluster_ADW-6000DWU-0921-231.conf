#load AZURE defaults
source "$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )/azure-saas_defaults.conf"

clusterID="$(get_id_cluster "$(basename $BASH_SOURCE)")"
clusterName="ADW-6000DWU-0921-${clusterID}"
numberOfNodes="30" #2000DWU #starts at 0 (max 99)
#Node config
vmSize='6000DWU-0921'

#details
vmCores="6"
vmRAM="42" #in GB

#costs
clusterCostHour="35.42"
clusterType="SaaS"
clusterDescription="Azure DW 6000DWU"

extraLocalCommands=""
