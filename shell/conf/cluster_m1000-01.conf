#load cluster defaults
source "$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )/cluster_defaults.conf"

#OLD Cluster config
defaultProvider="on-premise"

clusterID='01' #from 03 0 99
clusterName="m1000-01" #az
numberOfNodes="3" #starts at 0 (max 99) 0 is asigned to master

nodeName="minerva-1001 minerva-1002 minerva-1003 minerva-1004"

#Node config
#vmSize='A7'

#attachedVolumes="3"
#diskSize="1023"